# AGENTS.md

## 回复规范

- 默认使用中文，表达清晰、结构化，不过度解释。

## 代码修改

- 修改前先阅读项目结构、README、注释和相关实现，理解业务逻辑、数据流与依赖。
- 优先最小可行修改，避免不必要的大改和过度抽象。
- 修改功能、描述、图标或可见行为时，同步更新版本号和说明。
- 中大型修改前给出实施计划，包含修改文件、主要内容、风险和验证方式，获得确认后执行。已有明确授权按其范围继续。
- 开发优先级：功能正常、结构可维护、日志清晰、配置简单、README 易懂及时、命名与输出统一。
- 同一错误第二次出现时，先搜索 3–5 种可能解决方案，选择高效且风险低的方案。
- 给出结论或提交前确认依据、实际运行情况与测试结果，不以短时观察保证长期无故障。

## Git 与发布顺序

- 优先使用 SSH。
- 提交消息使用中文 Conventional Commits：`类型(范围): 简短中文描述`，空一行后用项目符号说明内容与原因。
- 每次更新必须先完成验证、提交并推送到 GitHub，确认远程目标分支包含待部署的确切提交后，才能更新服务器。
- GitHub 推送失败时，不得继续部署未推送提交。不得再通过 Git bundle、scp 或直接改服务器的方式绕过 GitHub 推送。
- 服务器优先从 GitHub 拉取已验证提交并进行快进更新；保留 `.env`、运行状态、未跟踪用户文件和回滚备份。
- 不强推、不覆盖远程已有工作。未获授权时，不因推送工作分支而自动合并到 main。
- 发布后核对本地、GitHub 和服务器提交号，按改动范围重启服务并验证实际运行状态；仅文档更新无需重启监控。
